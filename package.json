{
  "name": "notes",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run generate && bun run --cwd apps/web dev",
    "generate": "bun scripts/prebuild.ts",
    "build": "bun run build:pkg && bun run generate && bun run build:app",
    "build:pkg": "bun run --cwd packages/nuartz build",
    "build:app": "bun run --cwd apps/web build",
    "typecheck": "bun run --cwd packages/nuartz typecheck && bun run --cwd apps/web typecheck",
    "test": "bun run --cwd packages/nuartz test",
    "validate": "bun run build:pkg && bun scripts/validate-content.ts",
    "check": "bun run build:pkg && bun run generate && bun run typecheck && bun run test && bun scripts/validate-content.ts"
  },
  "version": "0.1.0",
  "description": "Ricardo's public Obsidian digital garden powered by Nuartz",
  "author": "Ricardo Lopes",
  "repository": {
    "type": "git",
    "url": "https://github.com/ricardo6142dh/notes.git"
  },
  "packageManager": "bun@1.3.10",
  "overrides": {
    "js-yaml": "3.15.0",
    "postcss": "8.5.25",
    "vite": "7.3.5",
    "sharp": "0.35.3",
    "lodash-es": "4.18.1",
    "uuid": "11.1.1",
    "svgo": "3.3.4",
    "picomatch": "4.0.5",
    "better-sqlite3": "13.0.2",
    "esbuild": "0.28.1"
  }
}
